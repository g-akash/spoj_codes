#include<iostream>
#include<string>

using namespace std;

int main()
{
	int n;
	string pos,s1,s2;
	cin>>pos;
	cin>>n;
	for(int i=0;i<n;i++)
	{
	cin>>s1>>s2;
	if(pos==s1)pos=s2;
	else if(pos==s2)pos=s1;
	}
}